# Отчёт по нарушениям .cursorrules (исправлено)

## Исправленные нарушения

### 1. **Кастомные inline-стили**
- **app-shell.tsx**: Убран `style={{ "--sidebar", "--sidebar-width", ... }}` у `SidebarProvider`. Добавлен класс `.sidebar-provider-app` в `globals.css` с теми же CSS-переменными.

### 2. **Жёстко заданные hex/цвета (green-600, amber-600, red-500)**
- **globals.css**: Добавлены токены `--success` (зелёный для статуса «Уже идёт») и `--warning` (янтарный для «Запланирован»), продублированы в `@theme` как `--color-success`, `--color-warning`.
- **app-shell.tsx**: Классы `bg-green-600/10 text-green-600` и т.п. заменены на константы из `lib/constants.ts`, использующие `bg-success/10 text-success`.
- **competitions-datatable.tsx**: Аналогично — статусы через константы и токен `success`.
- **competition-card-page-content.tsx**: `bg-red-500` → `bg-destructive`, `bg-amber-600/10 text-amber-600` → `bg-warning/10 text-warning`.

### 3. **Повторяющиеся классы без констант**
- **lib/constants.ts** (новый файл): Вынесены константы:
  - `BADGE_STATUS_ONGOING`, `BADGE_STATUS_PLANNED`, `BADGE_STATUS_COMPLETED`, `BADGE_STATUS_BASE`
  - `TAB_BADGE_INACTIVE`, `TAB_BADGE_ACTIVE`
  - `SELECT_PAGE_SIZE_TRIGGER_CLASS` (минимальная ширина селекта «Строк на странице»)
- **competition-participants-table.tsx**, **competitions-datatable.tsx**, **app-shell.tsx**: Используют эти константы вместо захардкоженных строк.

### 4. **Произвольные значения размеров**
- **globals.css**: Добавлены `--width-select-page-size: 6.5rem`, `--height-table-empty: 12.5rem`.
- **table-empty-state.tsx**: `h-[200px]` заменён на `h-[var(--height-table-empty)]`.
- Селект «Строк на странице» использует `min-w-[var(--width-select-page-size)]` через константу.

### 5. **Английский текст в UI (локализация)**
- **sign-in-form.tsx**: Все подписи и сообщения переведены на русский:
  - Заголовок «Welcome Back» → «С возвращением»
  - Подзаголовок, плейсхолдеры, лейблы («Email address*», «Password*», «Remember Me», «Forgot Password?», «Sign in», «New on our platform?», «Create an account»)
  - Сообщения валидации zod («Введите email», «Введите пароль», «Пароль не менее 6 символов» и т.д.)
- **dashboard-content.tsx**: В демо-данных `technologies: 'Figma & React'` → `'Figma и React'`.

---

## Что не трогалось (по правилам)

- **components/ui/** — не изменялись (оригиналы shadcn/ui). Там остаются:
  - inline-стили в `progress.tsx`, `sidebar.tsx`, `sonner.tsx`, `chart.tsx` (нужны для работы компонентов).
- **competitions-datatable.tsx**: Оставлен `style={{ width: `${header.getSize()}px` }}` у ячейки заголовка таблицы — ширина колонки задаётся react-table, альтернативы через один только CSS нет.
- **src/components/test/test-page-content.tsx** — тестовая/демо-страница с множеством нарушений (hex, gray-*, inline style, английский). Имеет смысл либо вынести в отдельный список правок, либо пометить блок как `// TODO: REMOVE BEFORE RELEASE`.
- **src/components/shadcn-studio/** — блоки из shadcn-studio с английским текстом и цветами вида `green-600`, `red-600` и т.д. По правилам после импорта блоков их нужно переносить и чистить; при необходимости можно пометить демо-строки как `// TODO: REMOVE BEFORE RELEASE` и постепенно заменять цвета на токены и текст на русский.

---

## Итог

- Добавлен **lib/constants.ts** с общими стилевыми константами.
- В **globals.css** добавлены токены **--success**, **--warning** и размеры **--width-select-page-size**, **--height-table-empty**, класс **.sidebar-provider-app**.
- Убраны inline-стили в app-shell, заменены жёстко заданные цвета на токены и константы в основных компонентах дашборда и карточки соревнования.
- Форма входа и видимый UI переведены на русский и используют токены/константы там, где это применимо.
